//sessionStorage
//tab-save, tab-term 로 저장한 모든 결과 잘 가져옴.(stored_Data 는 Object형태이나 stringify로 인해 json형 string임.
//var stored_Data = sessionStorage.getItem("tab-term");
//var p_data = JSON.parse(stored_Data);//json parse 를 쓰면 stringify 된 객체를 json 형태로 변환시킴
//console.log(p_data.birthday);//.키 이나 [키]으로 밸류에접근 가능.

$(document).ready(function(){
	
	
    
});